# ── Credenciales (NUNCA subir) ────────────────────────────────────────────────
.env
.env.*
.streamlit/secrets.toml

# ── Cache Python ──────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Streamlit: ignorar todo MENOS config.toml (config sí va al repo) ─────────
.streamlit/*
!.streamlit/config.toml

# ── Scripts de diagnóstico y debug locales (no parte del dashboard) ───────────
_*.py

# ── Archivos de sistema ───────────────────────────────────────────────────────
Thumbs.db
.DS_Store
*.tmp
*.log

# Modulo HHEE — estado de sesion de scrapers Playwright
_ctrlit_state.json
_rastreosat_state.json
_geocode_cache.json
_ctrlit_screenshots/
_rastreosat_screenshots/
